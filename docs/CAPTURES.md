# PII in captures

Worker screenshots may contain account numbers, names, OTPs on screen.

- Folder `captures/` is gitignored.
- Do not zip captures into git or public chat.
- Bank SKU: keep PNGs on the Worker disk; wipe with a local delete after the retention days.
- Decision logs must redact passwords and OTP digits.
