# Replay after headed proof

trajectories/*.jsonl records proved steps.
Replay is headed-visible only. Headless replay is not a default SKU.
UI drift → screenshot mismatch → invalidate cache → re-enter brain.
